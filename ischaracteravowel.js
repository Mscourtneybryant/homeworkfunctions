var prompt = require('prompt-sync')();


//isCharacterAVowel(ch)

function isCharacterAVowel(v){

var v = prompt("Enter a character to determine if the first letter is a vowel: "); 

if(v=='a' || v == 'e'|| v == 'i' || v =='o' || v =='u'){
    console.log('The letter is a vowel.');
}
else{
    console.log('The letter is a constant.');
}
}
return isCharacterAVowel();


 //isCharacterAVowel = (letter) => {
  //   if(letter
  //   = 'a' || 'e' || 'i' || 'o' || 'u' 


  console.log('--------------------------------------------------------------=----------------')
  // define a function sumArray and a function multiplyArray that sums and multiplies 
  //(respectively) all the numbers in an array of numbers. For example, sumArray([1,2,3,4]) should return 10, and multiplyArray([1,2,3,4]) should return 24.


 

